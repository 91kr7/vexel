# app-shell — Index

| Component | Type | Path | Responsibility | Spec |
|-----------|------|------|-----------------|------|
| Client bootstrap | configuration | `client/src/main.tsx` | The browser entry point: reads the factor the serving process is using under a bounded wait, adopts it as the page's timing scale, and only then reaches the application through a dynamic import — so no cadence is computed before the factor arrives, and a server that never answers leaves the application mounted at `1` rather than a blank page | `specs/client-bootstrap.md` |
| Navigation data | configuration | `client/src/shell/navigation.ts` | Defines the twelve screens, their grouping, glyphs and page-header copy; the last of them is the About screen, labelled for the operator while keeping its `coverage-matrix` id | `specs/navigation-data.md` |
| Shell | UI component | `client/src/shell/Shell.tsx` | Composes the frame, grouped navigation, page header (whose actions now carry the refresh control) and content area; owns the confirmation service and draws no failure of its own — neither the reported failures nor the lost connection, which the header pill alone tells, naming which of the server and the daemon is unreachable; binds connection status and asks the reload signal to watch the live channel, so a connection that comes back reads every mounted view again; and subscribes to no daemon event stream — that stream is the Dashboard's alone; tracks the active screen, restoring/persisting it via preferences (landing on the Dashboard when none is persisted) and exposing the analysis-cache size with its clear action; switches to the screen a pending cross-navigation request names; renders every screen's own content, the About screen's coverage matrix included, mounting no volume or network listing of its own — each is read by the screen showing it | `specs/shell.md` |
| RefreshControl | UI component | `client/src/shell/RefreshControl.tsx` | The top bar's refresh control: one press reloads what the server holds, then every mounted view, and waits for the live channel to have delivered what that reading produced, busy until all of it has ended, refusing a second press meanwhile, and reporting the outcome through the toast service | `specs/refresh-control.md` |
| Reload signal | frontend data client | `client/src/data/reload-signal.ts` | The application-wide "read it all again" broadcast: a view subscribes with its own read, one call ends only when every subscribed read has ended, and one is raised on its own each time the live channel starts delivering again | `specs/reload-signal.md` |
| Refresh client | frontend data client | `client/src/data/refresh-client.ts` | The typed `POST /api/refresh` call, resolving with what the server reloaded, skipped and failed to read | `specs/refresh-client.md` |
| useKeptReading | frontend hook | `client/src/data/use-kept-reading.ts` | Holds a reading together with its serialisation: an arrival equal to what is in hand replaces nothing, a different one replaces it, and a call serialises only what arrived — stored through by the container detail's inspect data and its process listing | `specs/use-kept-reading.md` |
| AboutNotice | UI component | `client/src/shell/AboutNotice.tsx` | The identity and legal notice: one self-contained block stating the product name, the author's copyright, the licence with a route to each of its two documents, the absence of warranty, the right to convey, the source repository with the running version beside it, the network-modification duty and the reservation of the name, under the About screen's one section-header treatment | `specs/about-notice.md` |
| Build-time version constant | configuration | `client/app-version.ts` | Injects the version declared by the repository root `package.json` into the client as `__APP_VERSION__`, for the app build and the unit-test run alike (`client/vite.config.ts`, `client/vitest.config.ts`, `client/src/app-version.d.ts`) | `specs/app-version-constant.md` |
| PlaceholderScreen | UI component | `client/src/shell/screens/PlaceholderScreen.tsx` | Stand-in content for an active id naming no screen of the navigation data (every screen now has its own content); hosts the REQ-6 destructive-confirmation demo | `specs/placeholder-screen.md` |
| ConfirmationProvider, useConfirmation | frontend service | `client/src/shell/services/ConfirmationService.tsx` | Destructive-confirmation service, owned by the Shell; also carries back the scope the human chose for an action that has one | `specs/confirmation-service.md` |
| ErrorReportingProvider, useErrorReporter | frontend service | `client/src/shell/services/ErrorReportingService.tsx` | Application-wide failure reporting: every report raises one danger toast carrying the daemon's own message and no action, holding no list of its own, and raises none at all while nothing is reachable | `specs/error-reporting-service.md` |
| FailedReadEmptyState, FAILED_READ_TITLE | UI component | `client/src/shell/FailedReadEmptyState.tsx` | The one wording a screen shows in place of data its read could not load: the same sentence whatever the cause, stating no cause and carrying no control | `specs/failed-read-empty-state.md` |
| useFailureReport | frontend hook | `client/src/shell/services/use-failure-report.ts` | Reports a failure a view holds as state through the reporting service, once per occurrence: the same message standing across a re-render reports nothing | `specs/use-failure-report.md` |
| ProgressProvider, useProgress | frontend service | `client/src/shell/services/ProgressService.tsx` | Application-wide pending/progress tracking service | `specs/progress-service.md` |
| ConnectionStatusProvider, useConnectionStatus | frontend service | `client/src/shell/services/ConnectionStatusService.tsx` | Daemon reachability, negotiated API version and CLI availability app-wide, read from the live channel with no clock and no request of its own, telling the two unreachable sides apart — the channel not delivering is the application server, a delivered status without the daemon is the daemon | `specs/connection-status-service.md` |
| DaemonEventStreamProvider, useDaemonEventStream | frontend service | `client/src/shell/services/EventStreamService.tsx` | Keeps the most recent live daemon events app-wide — read from the live channel — one entry per event even when it is delivered twice; mounted by `App`, read by the Dashboard's own stream panel alone and by no shell chrome | `specs/event-stream-service.md` |
| CrossNavigationProvider, useCrossNavigation | frontend service | `client/src/shell/services/CrossNavigationService.tsx` | Cross-screen navigation to a named object: a screen posts the request, the Shell switches screen, the destination reveals the object and acknowledges | `specs/cross-navigation-service.md` |
| App | UI component | `client/src/App.tsx` | Application root: wires the toast, connection-status, error-reporting, progress, event-stream and cross-navigation services around the Shell, the toast and connection services sitting above the reporting one | `specs/app.md` |
