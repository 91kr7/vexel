# images — Index

| Component | Type | Path | Responsibility | Spec |
|-----------|------|------|-----------------|------|
| ImagesService | backend service | `server/src/images/images-service.ts` | Lists local images over the Engine API (tags, digest, platform(s), size, age) and reads an image's inspect data (config, entrypoint/cmd, env, labels, exposed ports, history) plus the raw payload | `specs/images-service.md` |
| ImageTransferService | backend service | `server/src/images/image-transfer-service.ts` | Registry-facing image operations over the Engine API: pull and push with per-layer progress, tag, untag, remove, and prune of dangling images; also opens/consumes the browser-facing save/load tarball streams | `specs/image-transfer-service.md` |
| Images endpoints | REST endpoint | `server/src/images/images-routes.ts` | Exposes image listing, inspect, pull/push progress streams, save (browser download), load (browser upload), tag, untag, remove and prune to the client | `specs/images-endpoints.md` |
| Images client | frontend data client | `client/src/data/images-client.ts` | Typed `fetch` wrapper for the images endpoints, the pull/push progress stream URL builders, the save download URL builder and the load upload URL | `specs/images-client.md` |
| useImages | frontend hook | `client/src/data/use-images.ts` | Reads the image list, re-reading on a bounded poll and on `image` daemon events | `specs/use-images.md` |
| useImageInspect | frontend hook | `client/src/data/use-image-inspect.ts` | Reads a single image's inspect data, re-reading on `id` change and on `image` daemon events | `specs/use-image-inspect.md` |
| useImageTransferStream, useFileUpload | frontend hook | `client/src/data/use-image-transfer.ts` | Subscribes to a pull/push per-layer progress stream, or drives a generic local-file upload with byte progress and cancel (image load, container filesystem import), until completion or failure | `specs/use-image-transfer-stream.md` |
| Image layers client | frontend data client | `client/src/data/image-layers-client.ts` | Typed `fetch` wrapper for the layer stack and layer-to-build-cache traceability endpoints, and the changeset analysis progress-stream URL builder | `specs/image-layers-client.md` |
| useImageLayerStack | frontend hook | `client/src/data/use-image-layers.ts` | Reads a single image's layer stack, re-reading on `id` change and on `image` daemon events | `specs/use-image-layer-stack.md` |
| useImageChangesetStream | frontend hook | `client/src/data/use-image-changesets.ts` | Subscribes to the changeset analysis progress stream and collects its progress and final result, until completion or failure | `specs/use-image-changeset-stream.md` |
| useImageBuildCacheTrace | frontend hook | `client/src/data/use-image-build-cache-trace.ts` | Reads one image's layer-to-build-cache association, re-reading on `id` change | `specs/use-image-build-cache-trace.md` |
| Image signals client | frontend data client | `client/src/data/image-signals-client.ts` | Typed URL builder for the layer-efficiency and secret-signal analysis progress-stream endpoint | `specs/image-signals-client.md` |
| useImageSignalsStream | frontend hook | `client/src/data/use-image-signals.ts` | Subscribes to the layer-efficiency/secret-signal analysis progress stream and collects its progress and final result, until completion or failure | `specs/use-image-signals-stream.md` |
| Image filesystem client | frontend data client | `client/src/data/image-filesystem-client.ts` | Typed `fetch` wrapper for the filesystem extraction progress-stream URL builder, the lazy directory-listing call, and the in-tree metadata/content/search/export operations | `specs/image-filesystem-client.md` |
| useImageFilesystemExtraction | frontend hook | `client/src/data/use-image-filesystem-extraction.ts` | Subscribes to the filesystem extraction progress stream and collects its progress and final result, until completion or failure | `specs/use-image-filesystem-extraction.md` |
| useImageFilesystemTree | frontend hook | `client/src/data/use-image-filesystem-tree.ts` | Lazy per-directory queries over an already-extracted image filesystem | `specs/use-image-filesystem-tree.md` |
| useImageFilesystemEntryMetadata, useImageFilesystemEntryContent | frontend hook | `client/src/data/use-image-filesystem-entry.ts` | Reads one entry's metadata, and (for a file) its preview content with an operator mode override, re-reading on path/mode change | `specs/use-image-filesystem-entry.md` |
| useImageFilesystemSearch | frontend hook | `client/src/data/use-image-filesystem-search.ts` | Debounced, cancellable name/path search over an extracted tree, with bounded, cyclic next/previous match navigation | `specs/use-image-filesystem-search.md` |
| Image diff client | frontend data client | `client/src/data/image-diff-client.ts` | Typed `fetch` wrapper for the cross-image filesystem diff progress-stream URL builder and the lazy diff-tree directory-listing call | `specs/image-diff-client.md` |
| useImageDiffStream | frontend hook | `client/src/data/use-image-diff-stream.ts` | Subscribes to the cross-image diff comparison progress stream and collects its progress and final result, until completion or failure | `specs/use-image-diff-stream.md` |
| useImageDiffTree | frontend hook | `client/src/data/use-image-diff-tree.ts` | Lazy per-directory queries over the last compared diff tree for a pair of images | `specs/use-image-diff-tree.md` |
| ImagesScreen | UI component | `client/src/images/ImagesScreen.tsx` | The Images screen: toolbar (pull, prune dangling), searchable data table of local images laid out like the containers table, per-row run/tag/untag/push/remove actions with destructive confirmation on remove, pull/push progress dialogs, "run this image" opening the container create/run form pre-filled, and arrival on a named image/layer followed from a build-cache record | `specs/images-screen.md` |
| ImageDetailPanel | UI component | `client/src/images/ImageDetailPanel.tsx` | Image inspect surface: structured inspect data (config, env, labels, exposed ports, history) plus the raw payload, copyable, and actions opening the layer explorer (at a given layer when the caller names one) and the filesystem browser | `specs/image-detail-panel.md` |
| LayerExplorer | UI component | `client/src/images/LayerExplorer.tsx` | Per-image layer explorer: ordered layer stack with proportional size bars, shared and signals-findings markers, the build-cache record behind each layer (followable) or the stated reason there is none, per-layer added/modified/deleted paths behind a cost-warning/progress/cancel changeset analysis; can open pre-selected at a given layer | `specs/layer-explorer.md` |
| LayerEfficiencyView | UI component | `client/src/images/LayerEfficiencyView.tsx` | Per-image efficiency and secret-signal view: wasted bytes and efficiency score, deleted-later/overwritten files, duplicated content and flagged credential-looking paths, each navigating to its layer, under an explicit heuristic disclaimer | `specs/layer-efficiency-view.md` |
| FilesystemBrowser | UI component | `client/src/images/FilesystemBrowser.tsx` | Per-image, runtime-independent filesystem browser: cost-warning/progress/cancel extraction (an unstarted intermediate container, cleaned up whatever happens), the merged filesystem as a lazily expanded, searchable tree, an entry's metadata/content preview panel, and single-file/subtree download, with cache-source status and a re-extract action | `specs/filesystem-browser.md` |
| ImageDiffView | UI component | `client/src/images/ImageDiffView.tsx` | Cross-image filesystem diff view: pick two images, cost-warning/progress/cancel comparison, the difference as a navigable, status-filterable tree, and a selected changed path's aspects with a side-by-side content preview | `specs/image-diff-view.md` |
