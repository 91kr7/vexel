# containers — Index

| Component | Type | Path | Responsibility | Spec |
|-----------|------|------|-----------------|------|
| ContainersService | backend service | `server/src/containers/containers-service.ts` | Lists containers over the Engine API in name order, runs lifecycle operations and prune, reports each port mapping once and in a total order of its own (private, then public, then protocol) whatever the daemon's dual-stack duplication and unstable ordering, samples CPU/memory/network and the CPU capacity for running containers every 10 seconds while the demand registry has it started — all six figures out of the one stats frame it already fetched, withheld once a reading is older than three intervals — and reads/updates a container's full configuration (in place or by recreating it) | `specs/containers-service.md` |
| StatsDemandRegistry | backend service | `server/src/containers/stats-demand-registry.ts` | The count of live subscriptions to the sampled figures: zero to one starts the stats sampler and samples at once, one to zero stops it, and with zero the daemon is asked for nothing | `specs/stats-demand-registry.md` |
| ContainerCreateService | backend service | `server/src/containers/container-create-service.ts` | Creates a container over the Engine API from a full configuration, resolving the image first (local, or pulled with progress), create-only or create-and-start, reporting the daemon's own refusal message | `specs/container-create-service.md` |
| Container create endpoint | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes container creation to the client, streaming pull progress and then the creation outcome as newline-delimited JSON | `specs/container-create-endpoint.md` |
| Containers endpoints | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes container listing, lifecycle, rename, prune, inspect and configuration update to the client | `specs/containers-endpoints.md` |
| ContainerLogsService | backend service | `server/src/containers/container-logs-service.ts` | Streams a container's logs over the Engine API (stream selection, follow, timestamps, tail, since/until) as discrete lines, cancellable on consumer disconnect | `specs/container-logs-service.md` |
| Container logs endpoint | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes the container log stream to the client as server-sent events | `specs/container-logs-endpoint.md` |
| ContainerStatsService | backend service | `server/src/containers/container-stats-service.ts` | Streams a container's live resource usage over the Engine API, normalised into CPU %, memory used/limit, network in/out, block I/O and pid count, cancellable on consumer disconnect | `specs/container-stats-service.md` |
| Container stats endpoint | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes the container stats stream to the client as server-sent events | `specs/container-stats-endpoint.md` |
| Container stats subscription endpoint | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes the held connection a consumer proves itself with, registering it on open and releasing it on close — including a close nobody announced, discovered by the periodic write | `specs/container-stats-subscription-endpoint.md` |
| ContainerProcessesService | backend service | `server/src/containers/container-processes-service.ts` | Lists the processes running inside a container (pid, user, command, optional %CPU/%MEM) on demand | `specs/container-processes-service.md` |
| Container processes endpoint | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes the container process listing to the client | `specs/container-processes-endpoint.md` |
| ContainerExecService | backend service | `server/src/containers/container-exec-service.ts` | Opens an interactive command inside a running container over the Engine API as a duplex, resizable session, tearing down the exec instance on close | `specs/container-exec-service.md` |
| ContainerAttachService | backend service | `server/src/containers/container-attach-service.ts` | Attaches to a running container's own stdio over the Engine API as a duplex, resizable session, detachable without ever stopping the container | `specs/container-attach-service.md` |
| ContainerTransferService | backend service | `server/src/containers/container-transfer-service.ts` | Streams a container's filesystem out as a browser download and streams an uploaded filesystem tarball into an imported image | `specs/container-transfer-service.md` |
| Container transfer endpoints | REST endpoint | `server/src/containers/containers-routes.ts` | Exposes the container filesystem export download and image import upload to the client | `specs/container-transfer-endpoint.md` |
| Container interactive sessions endpoint | WebSocket endpoint | `server/src/containers/container-sessions-routes.ts` | Exposes the exec and attach sessions to the client as duplex WebSocket channels | `specs/container-sessions-endpoint.md` |
| Container stats client | frontend data client | `client/src/data/container-stats-client.ts` | Typed stats-sample/process shapes, the stats stream URL and the process-listing call | `specs/container-stats-client.md` |
| useContainerStats | frontend hook | `client/src/data/use-container-stats.ts` | Subscribes to a container's stats stream with a bounded, batch-flushed sample history and reconnection | `specs/use-container-stats.md` |
| useContainerProcesses | frontend hook | `client/src/data/use-container-processes.ts` | Reads a container's process listing once per container and on explicit refresh | `specs/use-container-processes.md` |
| Container logs client | frontend data client | `client/src/data/container-logs-client.ts` | Typed log-line/option shapes and the log stream URL builder | `specs/container-logs-client.md` |
| useContainerLogs | frontend hook | `client/src/data/use-container-logs.ts` | Subscribes to a container's log stream with a bounded, batch-flushed buffer, reconnection and a buffer snapshot | `specs/use-container-logs.md` |
| Containers client | frontend data client | `client/src/data/containers-client.ts` | Typed `fetch` wrapper for the containers endpoints | `specs/containers-client.md` |
| Container create client | frontend data client | `client/src/data/container-create-client.ts` | Typed creation configuration shapes and the creation call, reporting pull progress while it runs | `specs/container-create-client.md` |
| useContainerCreate | frontend hook | `client/src/data/use-container-create.ts` | Drives one creation: phase, pull progress, and the daemon's refusal exposed as state rather than thrown | `specs/use-container-create.md` |
| useContainers | frontend hook | `client/src/data/use-containers.ts` | Reads the container list, re-reading on a bounded poll and on `container` daemon events | `specs/use-containers.md` |
| useStatsSubscription | frontend hook | `client/src/data/use-stats-subscription.ts` | Holds the stats subscription open while its screen is mounted and the tab visible, releasing it on unmount and on the tab being hidden, with no unload-time signal of any kind | `specs/use-stats-subscription.md` |
| useContainerDetail | frontend hook | `client/src/data/use-container-detail.ts` | Reads a single container's inspect data, re-reading on `id` change and on `container` daemon events | `specs/use-container-detail.md` |
| Container session client | frontend data client | `client/src/data/container-session-client.ts` | Typed exec/attach launch options and the interactive-session WebSocket URL builder | `specs/container-session-client.md` |
| Container transfer client | frontend data client | `client/src/data/container-transfer-client.ts` | Typed export download URL builder (triggered with `triggerDownload`) and import upload URL builder, consumed with the images module's `useFileUpload` | `specs/container-transfer-client.md` |
| useContainerSession | frontend hook | `client/src/data/use-container-session.ts` | Duplex transport behind an exec/attach session: send/resize, a data subscription, explicit close on unmount | `specs/use-container-session.md` |
| ContainersScreen | UI component | `client/src/containers/ContainersScreen.tsx` | The Containers screen: toolbar (run container / create from image, prune stopped), filterable/searchable **grid of one card per container, three to a row**, per-card three fixed lifecycle slots plus an overflow menu (rename, export filesystem, kill, remove), inline rename, bulk prune, and the card's corner control opening that container's detail as a large-format dialog over the screen, at the format's fluid width so its property lists keep the columns the inline panel gave them | `specs/containers-screen.md` |
| ContainerCard | UI component | `client/src/containers/ContainerCard.tsx` | One container as one card, not itself clickable and carrying no selected state: state accent down the edge; dot and name at the left with the short id and the open-detail control anchored right, that control being the only route into the detail; the state pill beside the daemon's own uptime sentence; the image reference on a full-width field of its own, truncating at the front; CPU, MEMORY, NET I/O and PORTS stacked one per row with their capacities, fills, chips and *no sample* state; the four action slots in a footer of their own | `specs/container-card.md` |
| ContainerCreateForm | UI component | `client/src/containers/ContainerCreateForm.tsx` | The create/run form: image from the local images or typed freely, every configuration section, local validation, create-only / create-and-start, daemon refusal shown while the entered values are kept | `specs/container-create-form.md` |
| ContainerDetailPanel | UI component | `client/src/containers/ContainerDetailPanel.tsx` | The container detail as the body of the screen's dialog — no surface, chrome or dismissal of its own: Logs, Stats, Config (view/edit restart policy, limits, env, ports, mounts, health check, warning before a recreate), Processes, Inspect (structured data, the collapsible sections that have content, plus the raw payload shown as selectable text) and — for running containers — Exec/Attach tabs; the filesystem export having moved to the card's menu | `specs/container-detail-panel.md` |
| ContainerLogsView | UI component | `client/src/containers/ContainerLogsView.tsx` | The Logs tab: two control rows (daemon filters, then the stream's own row holding the search and `Download`), live tail, highlighted matches, download of the whole buffer | `specs/container-logs-view.md` |
| ContainerStatsView | UI component | `client/src/containers/ContainerStatsView.tsx` | The Stats tab: five live readings (CPU, memory, network, block I/O, pids) in five tracks, every tile carrying a meter and a sparkline over the recent samples | `specs/container-stats-view.md` |
| ContainerProcessesView | UI component | `client/src/containers/ContainerProcessesView.tsx` | The Processes tab: pid/user/command table with a manual refresh action | `specs/container-processes-view.md` |
| ContainerSessionView | UI component | `client/src/containers/ContainerSessionView.tsx` | The Exec/Attach tabs: launch form (exec) or attach action, live terminal, detach/close, session-ended state | `specs/container-session-view.md` |
