# system — Index

| Component | Type | Path | Responsibility | Spec |
|-----------|------|------|-----------------|------|
| DiskUsageService | backend service | `server/src/system/disk-usage-service.ts` | Reclaimable disk space broken down by stopped containers, dangling images, unused volumes, unused networks and build cache, each with its size and what it holds, plus the occupied-space breakdown by images, containers, volumes and build cache; owns the one held `/system/df` reading of the server — a refresh-cache kind on a 5-minute period, marked due by a volume's or container's `destroy` and by the application's own removals, that the per-volume sizes, the occupied-space breakdown and the dashboard's figures are all views of, so only the first caller ever waits for it | `specs/disk-usage-service.md` |
| SystemOverviewService | backend service | `server/src/system/overview-service.ts` | The host in one payload, assembled from held values alone so a repeated read costs the daemon and the CLI nothing: container counts and the image and volume counts from the held listings, the sizes from the held disk accounting, compose stacks, build cache with its active builder from the held inventories, and the occupied-space breakdown — a count and a size may therefore describe different moments | `specs/overview-service.md` |
| PruneService | backend service | `server/src/system/prune-service.ts` | Per-category prune and scoped system-wide prune through the existing per-area prunes, reporting what was removed and the space actually reclaimed, and marking the held disk accounting due once a run has succeeded | `specs/prune-service.md` |
| BaselineService | backend service | `server/src/system/baseline-service.ts` | The declared Engine API and docker CLI baseline the coverage statement refers to, next to the connected daemon's own versions and the comparison between them | `specs/baseline-service.md` |
| System endpoints | REST endpoint | `server/src/system/system-routes.ts` | Exposes the host overview, the reclaimable-space breakdown, the scoped prune and the declared baseline to the client | `specs/system-endpoints.md` |
| System client | frontend data client | `client/src/data/system-client.ts` | Typed `fetch` wrapper for the overview, disk-usage, prune and baseline endpoints | `specs/system-client.md` |
| useDiskUsage | frontend hook | `client/src/data/use-disk-usage.ts` | Holds the reclaimable-space breakdown, reading it on mount, after every prune, on the reload signal and on a context switch; drives the prunes | `specs/use-disk-usage.md` |
| SystemScreen | UI component | `client/src/system/SystemScreen.tsx` | The System & prune screen: **the eight daemon properties, which this screen keeps and keeps alone** (they describe the daemon, not a context — Contexts lost the block), in the product's property grid beside the reclaimable-space breakdown, with the per-category prune and the scoped system prune, each confirmed with the shared-daemon warning and reporting the space reclaimed | `specs/system-screen.md` |
