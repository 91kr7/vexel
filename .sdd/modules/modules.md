# Modules

| Module | Responsibility | Index |
|--------|----------------|-------|
| ui-library | The internal, domain-agnostic UI library (`client/src/ui/`): design tokens, the glass material, layout, navigation, control, metric and feedback primitives — the only place allowed raw DOM tags and CSS. | `ui-library/index.md` |
| app-shell | The application shell that composes the UI library: navigation data, the "Vexel — Docker Control" shell, and the application-wide confirmation/error/progress/connection/event services. | `app-shell/index.md` |
| docker-access | Server-side Docker access layer: the Engine API client (unix/TCP+TLS/ssh, version negotiation, typed errors) and the local `docker`/`compose`/`buildx` CLI runner — shared by every future domain area that talks to Docker. | `docker-access/index.md` |
| connectivity | Daemon reachability, negotiated API version and CLI/plugin availability: server status service and endpoint, and the client's typed status reader. | `connectivity/index.md` |
| events | The live daemon event stream: server subscription/republish/backlog, its SSE endpoint, and the client's subscription plus object-type invalidation registry. | `events/index.md` |
| local-persistence | Local, per-user persistence (preferences, console history, analysis-cache index) and host-path validation: the local store, the content-addressed analysis cache, the host-path validator, their endpoints, and the client's preferences client/hook. | `local-persistence/index.md` |
| containers | Container list, creation, lifecycle, inspection/configuration, logs, live statistics and processes: the Engine API listing/lifecycle/prune/inspect/config service and its bounded CPU/memory sampler, the creation service with image resolution and pull, the log- and stats-streaming services, the process listing, their endpoints, the client's containers data clients/hooks, the Containers screen with its create/run form, and its detail panel with the logs, stats and processes views. | `containers/index.md` |
| images | Image list and registry-facing actions: the Engine API listing/inspect service, the pull/push/tag/untag/remove/prune transfer service, their endpoints, the client's images data clients/hooks, the Images screen, its inspect detail panel, the layer explorer and the layer-efficiency/secret-signal view. | `images/index.md` |
| image-analysis | Per-image layer stack, per-layer changesets, layer-efficiency/duplicate/secret-signal findings, and runtime-independent filesystem browsing with in-tree file operations: manifest/config-derived layer metadata, cross-image shared-layer detection, cached and cancellable per-layer changeset computation with OCI whiteout awareness and content hashing, waste/duplicate-content/secret-pattern analysis derived from it, cached and cancellable filesystem extraction (an unstarted intermediate container, cleaned up whatever happens) with lazy tree reads, entry metadata/content/search/export over the extracted tree with containment against traversal and escaping symlinks, and their endpoints. | `image-analysis/index.md` |
| volumes | Volume list, inspect, create, remove and prune: the Engine API listing/inspect/create/remove/prune service merging in size and mounting-container data, its endpoints, and the client's volumes data client/hooks. | `volumes/index.md` |
| networks | Network list, inspect, create, remove, prune and container attach/detach: the Engine API listing/inspect/create/remove/prune/connect/disconnect service merging in attached-container data, its endpoints, and the client's networks data client/hooks. | `networks/index.md` |
| volumes-networks | The Volumes & networks screen: the two-panel layout with the Volumes panel (list, inline inspect, create, remove/prune) and the Networks panel (list, inline inspect, create, remove/prune, container attach/detach). | `volumes-networks/index.md` |
| compose | Compose project discovery, stack lifecycle and per-service scaling, compose file read/validated write/validation (the sole consumer of host-path validation, REQ-116), and aggregated per-service log streaming — all through the `docker compose` CLI channel — their endpoints, the client's compose data client/hooks, and the Compose screen. | `compose/index.md` |
| builders | buildx builder inventory and management (create, remove, select-active) and the build-cache inventory and prune — through the `docker buildx` CLI channel — their endpoints, the client's builders data client/hooks, and the Builders & cache screen. | `builders/index.md` |
| server-app | The server entrypoint that composes every server module into one running Express app. | `server-app/index.md` |
