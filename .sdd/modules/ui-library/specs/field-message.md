---
module: ui-library
component: FieldMessage
type: UI component
---

# FieldMessage

**Purpose** → field-level helper or validation message shown under a form control.

## Contract

- `<FieldMessage tone? children />`
  - `tone?: 'danger' | 'muted'` — visual emphasis (default `'danger'`).

## Requirements served

- plan-docker_management_app/REQ-25
