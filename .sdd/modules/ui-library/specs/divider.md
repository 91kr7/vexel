---
module: ui-library
component: Divider
type: UI component
---

# Divider

**Purpose** → a hairline separator between sections or list rows.

## Contract

- `<Divider orientation? />` — `orientation`: `'horizontal' | 'vertical'` (default `'horizontal'`).

## Requirements served

- plan-docker_management_app/REQ-3
