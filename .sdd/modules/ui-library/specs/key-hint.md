---
module: ui-library
component: KeyHint
type: UI component
---

# KeyHint

**Purpose** → a keyboard-shortcut hint rendered next to a control (e.g. "⌘K" for search).

## Contract

- `<KeyHint keys />` — `keys` is the literal shortcut text to display.

## Requirements served

- plan-docker_management_app/REQ-1
