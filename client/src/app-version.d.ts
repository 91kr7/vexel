// Build-time constant injected by the Vite and Vitest configs from the version
// declared by the repository root package.json; see client/app-version.ts for
// why that file is the only place the version lives.
declare const __APP_VERSION__: string;
